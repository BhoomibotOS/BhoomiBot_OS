{
  "name": "bhoomibot-live-relay",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket relay for the BhoomiBot robot<->operator live link. Forwards JSON control/telemetry and binary video frames within a session keyed by robotId + session code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
